version: "3.1"
services:
  #  mysql:
  #    image: mysql:latest
  #    environment:
  #      - MYSQL_ROOT_PASSWORD=test
  #      - MYSQL_DATABASE=test
  #      - MYSQL_USER=test
  #      - MYSQL_PASSWORD=test
  #    ports:
  #      - "3306:3306"

  #  phpmyadmin:
  #    image: phpmyadmin/phpmyadmin
  #    environment:
  #      - PMA_ARBITRARY=1
  #    ports:
  #      - "8181:80"

  webserver:
    image: php:apache
    working_dir: /var/www/html/
    volumes:
      - ./src:/var/www/html/
    ports:
      - "8080:80"
